```
		// 操作类型
		if(ds_biz.getValue("ldProcType")=="01") {
			// fixme: 交易日期
			/**
			 ** 如果操作类型为01-保函通知，默认交易日期。如果是02-保函更正通知，则自动带出。
			 **/
			// fixme: 保函编号
			/**
			 ** 如果操作类型为01-保函通知，报文：自动抓取报文20域值;信开：开立行保函编号，手工填入。如果操作类型为02-保函更正通知，则自动带出。
			 **/
			ds_biz.setValue("lgNo", "000000");
		}
```



```
		// 初始化时隐藏 回复电文TAB页，报文类型”选择 显示对应的 Tab 页
		document.getElementById("conte2").style.display= "none";
		document.getElementById("conte3").style.display= "none";
```

